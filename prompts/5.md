Implement the Legal communication flow: report generation, BU selection, and send. Still fully scripted, no backend.

PART A — Generate Communication Report (triggered from the detail page):
- On "Generate Communication Report", show a multi-line agentic generation sequence: AIThinkingIndicator with rotating captions ("Reading obligation mapping...", "Drafting executive summary...", "Generating FAQ...", "Identifying required evidence...") over ~2.5s total.
- Then reveal a structured report PREVIEW built from the seed data, with sections: Title, Executive Summary, What is changing, Why it matters, What you need to do (required actions), Obligations & Requirements, Impacted Business Units, Key Dates, Risks if not implemented, Support & escalation, Evidence required for audit, and an auto-generated FAQ (3-4 Q&As).
- The preview must be EDITABLE (a few key fields as editable text areas) and allow "Add Legal note". Include an "Approve & Send" primary button.
- Pre-fill all content from the REMIT II seed data so it looks like the agent wrote it from the prior workflow steps.

PART B — Select Business Units:
- After Approve, show the BU selection screen: a table of pre-mapped impacted BUs (from BusinessUnitImpact seed) with columns Business Unit, Impacted Process, Impact Level, Owner, Due Date, Impact Reason, and a Selected checkbox (pre-checked for impacted ones).
- Allow toggling selection and show the impact reason per BU. Note in the UI that the selection was "suggested by the system based on obligation mapping" — this is one of the agentic moments.

PART C — Send Notification:
- "Send Notification" button. On click: brief processing animation, then a confirmation: "Communication successfully sent to selected Business Units. Tracking and audit trail have been activated."
- This action must update in-memory app state so the Monitoring screen (next step) and the Business Unit dashboard reflect that notifications were sent and tickets created. Use a shared React context/store for the demo's mutable state (tickets, statuses, audit trail) seeded from src/data/ but mutable at runtime.
- Append corresponding entries to the audit trail.

Important: introduce the runtime store NOW (React context) so actions taken in the Legal view are visible in the Business Unit view and vice versa, since I'll switch roles live. Reset-on-reload is fine (no persistence).

Stop after send works and state propagates. Confirm that after sending, the Monitoring and BU dashboards would reflect the change.