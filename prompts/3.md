Build the application shell and navigation. The demo has TWO roles and you switch between them live during the presentation.

1. Create an AppLayout with:
   - A top bar showing BRAND.name, a global "Regulatory Change Management" title, and a prominent ROLE SWITCHER (segmented control: "Legal / Compliance" vs "Business Unit"). Switching role changes the sidebar and available routes.
   - A left sidebar whose items depend on the active role.
2. Manage active role in a simple React context (no external state library needed). Default to Legal/Compliance.
3. Legal/Compliance routes (stub pages for now, real content comes later):
   - /legal/dashboard
   - /legal/impact-overview
   - /legal/regulatory-change/:id   (detail)
   - /legal/monitoring
   - /legal/support-requests
   - /legal/audit
4. Business Unit routes (stubs):
   - /bu/dashboard
   - /bu/regulatory-change/:id   (detail, action-oriented)
   - /bu/tickets/:id
5. Make the role switcher route to the correct default dashboard when toggled.
6. Each stub page should render its title and a "coming soon" placeholder so navigation is fully clickable end-to-end.
7. Keep the /styleguide route available but unlinked.

Stop after navigation works for both roles. I should be able to click through every route without errors.