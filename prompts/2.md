Read the frontend-design skill if available, then build the shared UI layer for this demo. Goal: a polished, enterprise-but-modern look that impresses a prospect — NOT a default Tailwind template look.

Build reusable, typed components in src/components/:
1. StatusBadge — renders any workflow state with the color from the central state config (Step 1). Consistent everywhere.
2. Card / SectionCard — clean container with title, optional icon, optional actions.
3. DataTable — generic typed table with column config, supports a "selected" checkbox column and per-row status badges. Used for impact tables and dashboards.
4. RiskBadge / PriorityBadge.
5. Modal / SlideOver — for the AI chatbot panel and detail drawers.
6. Button variants (primary, secondary, ghost, danger) using BRAND colors from src/config/brand.ts.
7. AIThinkingIndicator — an animated "agent is working" indicator (pulsing dots / shimmer) used to fake live AI processing.
8. Timeline — vertical timeline component for the audit trail.
9. KpiWidget — dashboard stat tile (label, big number, optional trend/icon).

Design direction:
- Use BRAND.primaryColor and accentColor as the palette spine; neutral grays for surfaces.
- Clear typographic hierarchy, generous spacing, subtle borders/shadows.
- This is a demo: prioritize visual impact and clarity. Make state colors meaningful (e.g. red for overdue/escalated, green for completed/audit-ready, amber for in-progress/pending).
- Everything responsive enough to look good on a presentation screen (assume ~1440px).

Create a temporary /styleguide route that renders every component with sample props so I can review them in one place. Stop after this and show me the styleguide.