Implement the remaining Legal follow-up screens and the reminder/escalation mechanism. Scripted, store-driven.

1. /legal/monitoring (Communication & Follow-up Monitoring):
   - Per-BU table: Notification Status, Viewed, Downloaded, Action Status, Days to Due Date, Escalation flag — all read live from the store so it reflects what the BU did.
   - Status filters using the workflow states.

2. Reminder & escalation (demo-controllable, since it's scripted):
   - Add a small "Demo Controls" affordance (a discreet panel or button in the top bar, clearly for the presenter) with a "Simulate time: jump to T-30 days" action and subsequent jumps (T-14, T-7, T+1).
   - On each jump: update countdowns, fire scripted reminders to both Legal and BU (visible as notifications/toasts), show Due Soon / Overdue badges, and at T+1 trigger an Escalation (status Escalated) for any incomplete ticket, with an audit trail entry.
   - Keep this presenter control visually separate so it's obviously a demo device.

3. /legal/support-requests:
   - List SupportRequests from the store. Open one to see the question, the BU, the referenced report section, any chatbot transcript, priority, and a response box. "Send response" updates the request, optionally moves the ticket back to In Progress, and appends audit trail.

4. /legal/audit (Audit & Regulatory Reporting):
   - End-to-end Timeline of the REMIT II change (built from audit trail entries).
   - Per-BU audit table: Communication Sent, Viewed, Evidence Uploaded, Completed On, Due Date, Audit Status.
   - Evidence repository list (all evidence in the store, with links to reg change / BU / obligation).
   - A "Generate Regulatory Compliance & Audit Report" button: agentic generation animation, then a structured on-screen report (summary, obligations, impacted BUs, implementation status, evidence collected, communications, clarifications, escalations, audit trail, final compliance status) with an "Export" button that's a no-op + success toast (or triggers a browser print dialog).

Everything must stay consistent with the store. Stop and let me run the reminder/escalation sequence end to end.