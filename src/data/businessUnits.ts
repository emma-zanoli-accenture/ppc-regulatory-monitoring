import { BUSINESS_UNITS } from '../types';
import type { BusinessUnit } from '../types';

/** The selectable Business Units for communications (single fixed list). */
export const businessUnits: readonly BusinessUnit[] = BUSINESS_UNITS;
